SOURCES=$(wildcard ./*.cpp) vcglib/wrap/ply/plylib.cpp
OBJECTS=$(SOURCES:.cpp=.o)
PKG_CPPFLAGS=-I./vcglib
PKG_LIBS=$(shell "${R_HOME}/bin${R_ARCH_BIN}/Rscript.exe" -e "Rcpp:::LdFlags()")
PKG_CXXFLAGS=-std=c++0x
#PKG_CXXFLAGS= $(shell "${R_HOME}/bin${R_ARCH_BIN}/Rscript.exe" -e "Rcpp:::CxxFlags()") -std=c++0x

